# Copiar para .env.prod e preencher ANTES do primeiro `docker compose up`
# ATENÇÃO: senhas sem caracteres especiais (@, :, /, ?, #) — quebram URLs de conexão

ANTHROPIC_API_KEY=sk-ant-...

# Origins permitidos no CORS (backend aceita requests diretos se necessário)
CORS_ORIGINS=https://tradescan.klauberfischer.online
